# Espelho de [project.dependencies] em pyproject.toml
# (Streamlit Cloud, build portátil e scripts de launch).
# Dev local alternativo: pip install -e ".[dev]"
pandas>=2.0
pyarrow>=14.0
openpyxl>=3.1
duckdb>=1.0
streamlit>=1.35
streamlit-code-editor>=0.1.22
